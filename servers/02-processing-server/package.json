{
  "name": "@mcp-security-demo/processing-server",
  "version": "1.0.0",
  "description": "MCP Server 2 - Processing Server (Markdown parser with weak validation)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "markdown-it": "^14.0.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^13.0.7",
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.3"
  },
  "keywords": [
    "mcp",
    "security",
    "demo",
    "vulnerable",
    "markdown"
  ]
}
